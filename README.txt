NIVECO PRO 6.0 — Data Quality Fix

Changes:
- Removed eye symbol from the side drawer.
- Prevents incomplete or non-IPO auto-discovered records from being added.
- Cleans malformed auto-imported records from existing browser storage once.
- Unknown subscription, GMP, issue size and lot size display as — instead of 0.
- New auto-discovered IPOs are added only when company, dates, price and lot size are available.
- Drawer version updated to 5.7.

Note: Browser-only scraping cannot guarantee complete live IPO/GMP data. For production-grade auto updates, use a controlled backend feed.

Update 6.0.2: Browser refresh/start splash now shows only the NIVECO eye symbol. Header and drawer remain wordmark-only.

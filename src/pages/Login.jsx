import{signInWithEmailAndPassword,signInWithPopup}from'firebase/auth';import{useState}from'react';import{Navigate}from'react-router-dom';import{auth,googleProvider,isFirebaseConfigured}from'../lib/firebase';import Brand from'../components/Brand';
export default function Login({user}){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState('');if(user)return <Navigate to="/"/>;const submit=async e=>{e.preventDefault();try{await signInWithEmailAndPassword(auth,email,password)}catch(x){setError(x.message)}};return <main className="auth-page"><form className="login-card" onSubmit={submit}><Brand/><span className="eyebrow">WELCOME BACK</span><h1>Sign in to Insights</h1><p>Sync your watchlist and access real-time IPO alerts.</p>{!isFirebaseConfigured&&<div className="notice">Connect Firebase environment variables to enable authentication.</div>}<label>Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<div className="error">{error}</div>}<button className="primary" disabled={!isFirebaseConfigured}>Sign in</button><button type="button" className="google" disabled={!isFirebaseConfigured} onClick={()=>signInWithPopup(auth,googleProvider)}>Continue with Google</button></form></main>}

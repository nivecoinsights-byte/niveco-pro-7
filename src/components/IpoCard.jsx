import{Bell,Bookmark,CalendarDays}from'lucide-react';import{formatCurrency}from'../lib/ipo';
export default function IpoCard({ipo,watched,onWatch}){return <article className="ipo-card"><div className="card-top"><div className="company-logo">{ipo.symbol?.slice(0,2)||ipo.name?.slice(0,2)}</div><div><h3>{ipo.name}</h3><p>{ipo.type||'Mainboard'} • {ipo.exchange||'NSE, BSE'}</p></div><button className={`icon-btn ${watched?'active':''}`} onClick={()=>onWatch(ipo.id)} aria-label="Toggle watchlist"><Bookmark size={18} fill={watched?'currentColor':'none'}/></button></div><div className="status-row"><span className={`status ${ipo.status?.toLowerCase()}`}>{ipo.status}</span><span><CalendarDays size={14}/> {ipo.openDate} — {ipo.closeDate}</span></div><div className="metrics"><div><small>Price band</small><strong>{formatCurrency(ipo.priceMin)} – {formatCurrency(ipo.priceMax)}</strong></div><div><small>GMP</small><strong className="positive">{ipo.gmp?`+${formatCurrency(ipo.gmp)}`:'—'}</strong></div><div><small>Subscribed</small><strong>{ipo.subscription?`${ipo.subscription}×`:'—'}</strong></div></div><button className="card-action"><Bell size={15}/> Track updates</button></article>}
